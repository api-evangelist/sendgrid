generated: '2026-08-13'
method: searched
source: https://github.com/twilio/ai/tree/main/skills/sendgrid
description: >-
  Provider-published Agent Skills for Twilio SendGrid. These are NOT generated by
  the API Evangelist pipeline — they are the SKILL.md files Twilio ships in the
  public github.com/twilio/ai repository under skills/sendgrid/, saved here
  VERBATIM (byte-for-byte as served by the GitHub contents API on 2026-08-13).
  Twilio announced them alongside the Twilio MCP server in "Introducing the
  Twilio MCP Server and Skills" (2026-05-07) and distributes them as the
  `twilio-developer-kit` plugin for Claude Code, Cursor and OpenCode.
  SendGrid is a Twilio company and its developer documentation is served from
  www.twilio.com/docs/sendgrid, so a SendGrid skill published in the Twilio AI
  repo is first-party.
publisher:
  name: Twilio (Twilio SendGrid)
  repo: https://github.com/twilio/ai
  path: skills/sendgrid
  license: see https://github.com/twilio/ai/blob/main/LICENSE
  distribution:
    - Claude Code plugin `twilio-developer-kit`
    - Cursor plugin marketplace (`/add-plugin twilio-developer-kit`)
    - direct clone of github.com/twilio/ai
  announcement: https://www.twilio.com/en-us/blog/developers/introducing-twilio-mcp-skills
companion_mcp: mcp/sendgrid-mcp.yml
skills:
  - name: twilio-sendgrid-account-setup
    file: skills/sendgrid-account-setup.md
    upstream: https://github.com/twilio/ai/blob/main/skills/sendgrid/twilio-sendgrid-account-setup/SKILL.md
    summary: >-
      Set up a SendGrid account for email delivery — API key creation (SG.-prefix),
      domain authentication (DKIM/SPF via CNAME), Single Sender Verification for
      testing, SDK installation.
    api: SendGrid v3
    operations:
      - GET /v3/whitelabel/domains
      - GET /v3/whitelabel/domains/{id}/validate
    openapi: openapi/sendgrid-domain-authentication-api-openapi.yml
  - name: twilio-sendgrid-email-send
    file: skills/sendgrid-email-send.md
    upstream: https://github.com/twilio/ai/blob/main/skills/sendgrid/twilio-sendgrid-email-send/SKILL.md
    summary: >-
      Send transactional and bulk email via the v3 Mail Send API — single sends,
      personalized batch sends with dynamic templates, scheduled sends with
      cancellation, attachments, sandbox mode.
    api: SendGrid v3
    operations:
      - POST /v3/mail/send
    openapi: openapi/sendgrid-mail-send-api-openapi.yml
  - name: twilio-sendgrid-webhooks
    file: skills/sendgrid-webhooks.md
    upstream: https://github.com/twilio/ai/blob/main/skills/sendgrid/twilio-sendgrid-webhooks/SKILL.md
    summary: >-
      Track delivery and engagement via Event Webhooks — the 11 event types,
      handler implementation, ECDSA signature verification, batched event
      processing.
    api: SendGrid Event Webhook
    asyncapi: asyncapi/sendgrid-event-webhook-asyncapi.yml
  - name: twilio-sendgrid-inbound-parse
    file: skills/sendgrid-inbound-parse.md
    upstream: https://github.com/twilio/ai/blob/main/skills/sendgrid/twilio-sendgrid-inbound-parse/SKILL.md
    summary: >-
      Receive inbound email via the Inbound Parse webhook — MX record setup,
      parsed vs raw mode, attachment handling, common pitfalls.
    api: SendGrid Inbound Parse
    asyncapi: asyncapi/sendgrid-inbound-parse-asyncapi.yml
  - name: twilio-sendgrid-suppressions
    file: skills/sendgrid-suppressions.md
    upstream: https://github.com/twilio/ai/blob/main/skills/sendgrid/twilio-sendgrid-suppressions/SKILL.md
    summary: >-
      Manage suppressions — bounces, blocks, spam reports, invalid emails, global
      unsubscribes and ASM suppression groups; when and how to remove them.
    api: SendGrid v3
    openapi: openapi/sendgrid-suppressions-api-openapi.yml
  - name: twilio-sendgrid-email-settings
    file: skills/sendgrid-email-settings.md
    upstream: https://github.com/twilio/ai/blob/main/skills/sendgrid/twilio-sendgrid-email-settings/SKILL.md
    summary: >-
      Configure dynamic templates (Handlebars), tracking settings (opens, clicks,
      subscriptions), link branding for custom tracking domains, and content types
      (HTML, plain text, AMP).
    api: SendGrid v3
    operations:
      - GET /v3/whitelabel/links/{id}/validate
    openapi: openapi/sendgrid-link-branding-api-openapi.yml
  - name: twilio-sendgrid-engagement-quality
    file: skills/sendgrid-engagement-quality.md
    upstream: https://github.com/twilio/ai/blob/main/skills/sendgrid/twilio-sendgrid-engagement-quality/SKILL.md
    summary: >-
      Monitor email program health with SendGrid Engagement Quality (SEQ) scores —
      the SEQ API endpoints and the five scoring metrics.
    api: SendGrid v3
    operations:
      - GET /v3/engagementquality/scores
      - GET /v3/engagementquality/subusers/scores
    openapi: openapi/sendgrid-engagement-quality-api-openapi.yml
  - name: twilio-sendgrid-deliverability-advisor
    file: skills/sendgrid-deliverability-advisor.md
    upstream: https://github.com/twilio/ai/blob/main/skills/sendgrid/twilio-sendgrid-deliverability-advisor/SKILL.md
    summary: >-
      Diagnostic/advisory skill for deliverability problems — spam foldering,
      blocks, bounces, sender reputation. Carries a `tier` frontmatter key the
      other seven skills do not.
    api: SendGrid v3
    note: >-
      Advisory rather than operation-bound; it reasons over the deliverability
      surface instead of naming a single endpoint set.
x-evidence:
  fetched: '2026-08-13'
  method: GitHub contents API (repos/twilio/ai/contents/skills/sendgrid)
  files_saved: 8
  verbatim: true
